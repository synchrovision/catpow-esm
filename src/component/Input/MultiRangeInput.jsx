import React from "react";
import ReactDOM from "react-dom";
import { useScratch } from "react-use";

export const MultiRangeInput = (props) => {
	const { className = "cp-multirangeinput", width = 100, snap = false, x = 50, y = 50, r = 6, onChange, ...otherProps } = props;
	const [ref, state] = useScratch();
};
